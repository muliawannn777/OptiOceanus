// ai.js - Logika AI sederhana (TensorFlow.js atau custom)

// Contoh fungsi AI sederhana
function predictWaveCondition(input) {
    // Placeholder prediksi, ganti dengan model AI sesungguhnya
    return input > 2 ? 'Tinggi' : 'Rendah';
}
